{
  "functions": {
    "api/*.php": {
      "runtime": "vercel-php@0.7.3"
    }
  },
  "routes": [{ "src": "/api/(.*)", "dest": "/api/$1" }],
  "builds": [
    {
      "src": "composer.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "vendor"
      }
    }
  ]
}
